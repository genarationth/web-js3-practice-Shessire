const addNum = (a, b) => {
    return (a + b);
}
addNum(2, 4);

const subNum = (a, b) => {
    return (a - b);
}
subNum(2, 4);

const multiNum = (a, b) => {
    return (a * b);
}
multiNum(2, 4);

const divNum = (a, b) => {
    return (a / b);
}
divNum(2, 4);

const aboutMe = (name,location,hobby) => {
    console.log(`Hi, my name is ${name}. I live in ${location} and enjoy ${hobby}`);
}

aboutMe('Pond', 'Thailand', 'playing video games');